import { StyleSheet } from "react-native";

export const arrowBackStyles = StyleSheet.create({
  ButtonContainer: {
    borderWidth: 1,
    borderColor: "#ccc",
    height: 50,
    width: 50,
    borderRadius: 25,
    alignItems: "center",
    justifyContent: "center",
    marginBottom: 20, 
    alignSelf: "flex-start"
  },
});
